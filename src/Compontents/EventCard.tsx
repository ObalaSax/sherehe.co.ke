import { Grid } from "@mui/material";

function EventCard() {
  return (
    <Grid item xs={12} md={3} lg={4} border={1} margin={1} borderRadius={1}>
      Event Card
    </Grid>
  );
}

export default EventCard;
