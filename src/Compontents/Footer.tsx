import { Box, Typography } from "@mui/material";

function Footer() {
  return (
    <Box position={"fixed"} marginBottom={0} width={100}>
      <Typography variant="h1" color="initial">
        Noma
      </Typography>
    </Box>
  );
}

export default Footer;
